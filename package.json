{
  "name": "quantagent",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "oxlint apps/web",
    "lint:fix": "oxlint --fix apps/web",
    "fmt": "oxfmt apps/web",
    "fmt:check": "oxfmt --check apps/web"
  },
  "devDependencies": {
    "oxfmt": "^0.49.0",
    "oxlint": "^1.64.0"
  }
}